<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WordleByShik" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimaryDark">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.WordleByShik" parent="Base.Theme.WordleByShik" />

    <!-- Основные стили приложения -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Стиль для закругленного TextView -->
    <style name="RoundedTextView">
        <item name="android:background">@drawable/rounded_background</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">18sp</item>
        <item name="android:padding">5dp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>
</resources>